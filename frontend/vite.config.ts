import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    proxy: {
      // Frontend calls /api/* ; backend routes have no prefix (e.g. /companies/{ticker}/wiki).
      // Proxying avoids needing CORS for local dev -- production (Phase 8) will need real
      // CORS config once frontend/backend are on different deployed domains.
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ''),
      },
    },
  },
})
