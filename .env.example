GEMINI_API_KEY=
DATABASE_URL=postgresql+psycopg2://trader:trader@localhost:5433/trader
FINNHUB_API_KEY=
ALPHA_VANTAGE_API_KEY=
# Groq (second AI provider, multi-horizon forecast) -- DORMANT as of 2026-08-05: the user
# cannot currently sign in to Groq to obtain a key. Leave this blank and the forecast feature
# stays switched off with no effect on startup, existing routes, scheduled jobs, tests, or
# migrations (spec.md NFR-9/FR-33a). Dropping a key in here is the only step needed to activate
# it (plus re-checking groq_model and running scripts/test_groq_prompt.py -- see spec.md's
# "Groq activation" checklist).
GROQ_API_KEY=
